package com.xcdm.livein.repo;

import com.xcdm.livein.entity.Texture;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TextureRepository extends JpaRepository<Texture, Integer> {
}